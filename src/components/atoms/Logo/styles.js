import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
  },
  img: {
    marginBottom: 20,
  },
  label: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#ffffff',
  },
});

export default styles;
